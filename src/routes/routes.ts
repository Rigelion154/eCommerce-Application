const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  REGISTRATION: '/registration',
  PROFILE: '/profile',
  CART: '/cart',
  CATEGORIES: '/categories/:slug',
  SUBCATEGORIES: '/categories/:slug/:slug',
};

export default ROUTES;
